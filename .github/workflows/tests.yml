name: Tests

on:
  push:
    branches: [master, main]
  pull_request:
    branches: ['**']

jobs:
  test:
    timeout-minutes: 10
    name: Bun on ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Run linter
        run: bun run lint
        continue-on-error: true

      - name: Run tests
        run: |
          bun test tests/unit --timeout 20000
          bun test tests/integration --timeout 20000
          bun test tests/end-to-end --timeout 20000
        env:
          CI: true

  build:
    timeout-minutes: 10
    name: Build browser bundles
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build
        run: bun run build

      - name: Check dist files exist
        run: |
          test -f dist/excelts.js
          test -f dist/excelts.min.js

      - name: Run dist and browser tests
        run: |
          bun test tests/dist --timeout 20000
          bun test tests/browser --timeout 20000
        env:
          CI: true

  typecheck:
    timeout-minutes: 10
    name: TypeScript type checking
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Type check
        run: bunx tsc --noEmit
        continue-on-error: true  # TODO: Fix 2800+ pre-existing type errors
        env:
          CI: true
